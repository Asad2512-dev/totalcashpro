TotalCashPro — Hostinger Deploy (public_html structure)
=======================================================

Repo: https://github.com/Asad2512-dev/totalcashpro
Branch: main
SSH: ssh -p 65002 u424452004@82.197.83.28

IMPORTANT — PHP 8.4 required (Laravel 13 / Symfony 8.1)
---------------------------------------------------------
1) hPanel → Websites → totalcashpro.com → Advanced → PHP Configuration → PHP 8.4 → Update
2) SSH must also use PHP 8.4 (default `php` is often 8.3):

   export PATH="/opt/alt/php84/usr/bin:$PATH"
   php -v

   If php84 folder missing, try:
   export PATH="/opt/alt/php85/usr/bin:$PATH"

   Make permanent (optional):
   echo 'export PATH="/opt/alt/php84/usr/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc

Project path:
  ~/domains/totalcashpro.com/public_html/

Daily update (after git pull):
  cd ~/domains/totalcashpro.com/public_html
  export PATH="/opt/alt/php84/usr/bin:$PATH"
  php -v
  git pull origin main
  composer install --no-dev --optimize-autoloader
  php artisan migrate --force
  php artisan config:cache
  php artisan route:cache
  php artisan view:cache
  chmod -R 775 storage bootstrap/cache

First-time / after big pull:
  cp .env.example .env   # only if .env missing
  php artisan key:generate
  php artisan migrate --force
  php artisan db:seed    # optional demo data only

Live .env essentials:
  APP_ENV=production
  APP_DEBUG=false
  APP_URL=https://totalcashpro.com
  ASSET_URL=

Super Admin (after seed): admin@totalcashpro.com / admin123
